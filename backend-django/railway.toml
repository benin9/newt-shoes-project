[build]
builder = "NIXPACKS"

[build.args]
NIXPACKS_POETRY_VERSION = ""

[deploy]
startCommand = "gunicorn core.wsgi:application --bind 0.0.0.0:$PORT --workers 1 --preload --timeout 300 --access-logfile - --error-logfile -"

[deploy.config]
healthcheckPath = "/"
